from setuptools import setup, find_packages

setup(name='npugraph_ex',
      version='0.1',
      description='NPU graph extension',
      long_description='NPU graph extension',
      packages=find_packages(),
      include_package_data=True,
      ext_modules=[],
      install_requires=[],
      zip_safe=False)
